# 交易感知系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立"看盘→做功课→验证记录"交易感知闭环的三条链路，先跑通核心链路再迭代

**Architecture:** FastAPI 路由拆分 + SQLite 新增表 + 纯 NumPy 技术指标 + 前端 ECharts 副图。不引入新依赖，复用现有 httpxt/cache/行情管道

**Tech Stack:** Python/FastAPI/sqlite3/NumPy/ECharts，零新依赖

---

## File Structure

```
src/
├── database.py              → 修改：新增 4 张表 + watchlist 加字段
├── stock_utils.py           → 已修复，不动
├── services/
│   └── indicators.py        → 新建：RSI/MACD/KDJ/BOLL
├── routers/
│   ├── __init__.py           → 新建
│   ├── watchlist.py          → 新建：自选股 CRUD
│   ├── sim.py               → 新建：模拟买卖
│   ├── journal.py           → 新建：交易日记
│   ├── verify.py            → 新建：博主追踪 + 真实账户 [钩子]
│   └── stats.py             → 新建：盈亏统计
├── app.py                   → 修改：拆出已有路由 + 注册新路由
├── stock_service.py         → 修改：K线端点加 indicators 参数
└── templates/
    └── index.html           → 修改：自选Tab + 指标副图 + 模拟交易页面
```

---

### Task 1: 数据库建表

**Files:**
- Modify: `src/database.py` — 在 `init_db()` 的 `executescript` 末尾追加 SQL

- [ ] **Step 1: 在 database.py 中追加建表 SQL**

找到 `init_db()` 中 `_db.executescript("""...""")` 语句，在最后一个 `);` 之后、`"""` 之前追加以下 SQL（位置在 backtests 表定义后）：

```sql
            -- watchlist 新增字段
            ALTER TABLE watchlist ADD COLUMN tags TEXT DEFAULT '';
            ALTER TABLE watchlist ADD COLUMN alert_price REAL;
            ALTER TABLE watchlist ADD COLUMN target_price REAL;

            CREATE TABLE IF NOT EXISTS trade_journal (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                symbol      TEXT NOT NULL,
                market      TEXT NOT NULL,
                direction   TEXT NOT NULL,
                entry_date  TEXT,
                exit_date   TEXT,
                entry_price REAL,
                exit_price  REAL,
                quantity    REAL,
                reason      TEXT,
                reflection  TEXT,
                tags        TEXT,
                created_at  TEXT DEFAULT (datetime('now'))
            );

            CREATE TABLE IF NOT EXISTS sim_trades (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                symbol      TEXT NOT NULL,
                market      TEXT NOT NULL,
                direction   TEXT NOT NULL,
                price       REAL NOT NULL,
                quantity    REAL NOT NULL,
                fee         REAL DEFAULT 0,
                trade_date  TEXT NOT NULL,
                status      TEXT DEFAULT 'open',
                closed_at   TEXT,
                close_price REAL,
                pnl         REAL,
                note        TEXT,
                created_at  TEXT DEFAULT (datetime('now'))
            );

            CREATE TABLE IF NOT EXISTS blogger_calls (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                blogger_id  TEXT NOT NULL,
                symbol      TEXT NOT NULL,
                market      TEXT NOT NULL,
                call_type   TEXT,
                call_date   TEXT NOT NULL,
                call_price  REAL,
                target_price REAL,
                article_url TEXT,
                notes       TEXT,
                verified    INTEGER DEFAULT 0,
                verified_at TEXT,
                created_at  TEXT DEFAULT (datetime('now'))
            );

            CREATE TABLE IF NOT EXISTS real_trades (
                id          INTEGER PRIMARY KEY AUTOINCREMENT,
                symbol      TEXT NOT NULL,
                market      TEXT NOT NULL,
                direction   TEXT NOT NULL,
                price       REAL NOT NULL,
                quantity    REAL NOT NULL,
                fee         REAL DEFAULT 0,
                trade_date  TEXT NOT NULL,
                source      TEXT DEFAULT 'manual',
                note        TEXT,
                created_at  TEXT DEFAULT (datetime('now'))
            );
```

注意：`ALTER TABLE watchlist ADD COLUMN` 会在已存在的表上失败（列已存在）。用 `try/except` 包装 ALTER 语句更健壮，但 SQLite 没有存储过程。改方案——把 ALTER 语句从 executescript 中移出，在 init_db() 里单独用 try/except 执行。

具体做法：在 `close_db` 上方加一个辅助函数 `_migrate()` ，在 `_db.executescript(...)` 之后调用：

```python
def _migrate():
    """增量迁移：添加新列。忽略 'duplicate column name' 错误。"""
    migrations = [
        "ALTER TABLE watchlist ADD COLUMN tags TEXT DEFAULT ''",
        "ALTER TABLE watchlist ADD COLUMN alert_price REAL",
        "ALTER TABLE watchlist ADD COLUMN target_price REAL",
    ]
    for sql in migrations:
        try:
            _db.execute(sql)
        except sqlite3.OperationalError:
            pass  # 列已存在
```

然后在 `init_db()` 中 `_db.commit()` 之前调用 `_migrate()`。

- [ ] **Step 2: 删除旧 data.db 验证建表**

```bash
cd src && rm -f ../data.db && python -c "
from database import init_db, get_db
init_db()
db = get_db()
tables = db.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'\").fetchall()
print('Tables:', [t[0] for t in tables])
for t in tables:
    cols = db.execute(f'PRAGMA table_info({t[0]})').fetchall()
    print(f'  {t[0]}:', [c[1] for c in cols])
"
```

Expected: 9 tables including `trade_journal`, `sim_trades`, `blogger_calls`, `real_trades`, with `tags/alert_price/target_price` in watchlist.

- [ ] **Step 3: Commit**

```bash
git add src/database.py
git commit -m "feat: 新增交易系统DB表 — 自选加字段 + 日记/模拟/博主追踪/真实账户"
```

---

### Task 2: 技术指标计算模块

**Files:**
- Create: `src/services/__init__.py` (空文件)
- Create: `src/services/indicators.py`

- [ ] **Step 1: 创建 src/services/__init__.py**

```bash
touch src/services/__init__.py
```

- [ ] **Step 2: 创建 src/services/indicators.py**

```python
"""技术指标计算 — 纯 NumPy 实现，不依赖 talib。"""

import numpy as np


def calc_rsi(close: list[float], period: int = 14) -> list[float]:
    """RSI 相对强弱指标，返回与输入等长的 list（前期填 None）。"""
    n = len(close)
    if n < period + 1:
        return [None] * n

    close_arr = np.array(close, dtype=float)
    delta = np.diff(close_arr, prepend=close_arr[0])
    gain = np.where(delta > 0, delta, 0.0)
    loss = np.where(delta < 0, -delta, 0.0)

    avg_gain = np.full(n, np.nan)
    avg_loss = np.full(n, np.nan)
    avg_gain[period] = gain[1:period+1].mean()
    avg_loss[period] = loss[1:period+1].mean()

    for i in range(period + 1, n):
        avg_gain[i] = (avg_gain[i-1] * (period - 1) + gain[i]) / period
        avg_loss[i] = (avg_loss[i-1] * (period - 1) + loss[i]) / period

    rs = avg_gain / np.where(avg_loss == 0, 1e-10, avg_loss)
    rsi = 100.0 - (100.0 / (1.0 + rs))

    result = [None] * period + rsi[period:].tolist()
    return result


def calc_macd(close: list[float], fast: int = 12, slow: int = 26, signal: int = 9):
    """MACD 指标。返回 {dif, dea, macd}，各为等长 list（前期填 None）。"""
    n = len(close)
    if n < slow:
        return {"dif": [None]*n, "dea": [None]*n, "macd": [None]*n}

    close_arr = np.array(close, dtype=float)

    def ema(data, period):
        alpha = 2.0 / (period + 1)
        result = np.full(len(data), np.nan)
        result[period-1] = data[:period].mean()
        for i in range(period, len(data)):
            result[i] = alpha * data[i] + (1 - alpha) * result[i-1]
        return result

    ema_fast = ema(close_arr, fast)
    ema_slow = ema(close_arr, slow)
    dif = ema_fast - ema_slow
    dea = ema(dif, signal)
    macd = 2.0 * (dif - dea)

    start = slow + signal - 2
    return {
        "dif": [None]*start + dif[start:].tolist(),
        "dea": [None]*start + dea[start:].tolist(),
        "macd": [None]*start + macd[start:].tolist(),
    }


def calc_kdj(high: list[float], low: list[float], close: list[float], n: int = 9) -> dict:
    """KDJ 指标。返回 {k, d, j}，各为等长 list。"""
    length = len(close)
    if length < n:
        return {"k": [None]*length, "d": [None]*length, "j": [None]*length}

    high_arr = np.array(high, dtype=float)
    low_arr = np.array(low, dtype=float)
    close_arr = np.array(close, dtype=float)

    k = np.full(length, np.nan)
    d = np.full(length, np.nan)
    j = np.full(length, np.nan)

    k[n-1] = 50.0
    d[n-1] = 50.0

    for t in range(n, length):
        hh = high_arr[t-n+1:t+1].max()
        ll = low_arr[t-n+1:t+1].min()
        rsv = (close_arr[t] - ll) / (hh - ll + 1e-10) * 100.0
        k[t] = 2.0/3.0 * k[t-1] + 1.0/3.0 * rsv
        d[t] = 2.0/3.0 * d[t-1] + 1.0/3.0 * k[t]
        j[t] = 3.0 * k[t] - 2.0 * d[t]

    return {
        "k": [None]*(n-1) + k[n-1:].tolist(),
        "d": [None]*(n-1) + d[n-1:].tolist(),
        "j": [None]*(n-1) + j[n-1:].tolist(),
    }


def calc_boll(close: list[float], period: int = 20, std: int = 2) -> dict:
    """布林带。返回 {mid, upper, lower}，各为等长 list。"""
    n = len(close)
    if n < period:
        return {"mid": [None]*n, "upper": [None]*n, "lower": [None]*n}

    close_arr = np.array(close, dtype=float)
    mid = np.full(n, np.nan)
    upper = np.full(n, np.nan)
    lower = np.full(n, np.nan)

    for i in range(period - 1, n):
        window = close_arr[i-period+1:i+1]
        m = window.mean()
        s = window.std(ddof=1)
        mid[i] = m
        upper[i] = m + std * s
        lower[i] = m - std * s

    return {
        "mid": [None]*(period-1) + mid[period-1:].tolist(),
        "upper": [None]*(period-1) + upper[period-1:].tolist(),
        "lower": [None]*(period-1) + lower[period-1:].tolist(),
    }
```

- [ ] **Step 3: 验证指标计算**

```bash
cd src && python -c "
import numpy as np
from services.indicators import calc_rsi, calc_macd, calc_kdj, calc_boll

# 模拟 50 根 K 线
np.random.seed(42)
close = (np.cumsum(np.random.randn(50) * 0.5) + 100).tolist()
high = [c + abs(np.random.randn()*0.3) for c in close]
low = [c - abs(np.random.randn()*0.3) for c in close]

rsi = calc_rsi(close)
assert len(rsi) == 50
assert any(v is not None for v in rsi), 'RSI should have values after period'

macd = calc_macd(close)
assert len(macd['dif']) == 50
assert any(v is not None for v in macd['dif']), 'MACD DIF should have values'

kdj = calc_kdj(high, low, close)
assert len(kdj['k']) == 50
assert any(v is not None for v in kdj['k']), 'KDJ K should have values'

boll = calc_boll(close)
assert len(boll['mid']) == 50
assert any(v is not None for v in boll['mid']), 'BOLL mid should have values'

print('All indicators OK')
"
```

Expected: `All indicators OK`

- [ ] **Step 4: Commit**

```bash
git add src/services/
git commit -m "feat: 技术指标计算模块 — RSI/MACD/KDJ/BOLL 纯NumPy实现"
```

---

### Task 3: K线 API 集成指标

**Files:**
- Modify: `src/stock_service.py` — `get_kline()` 加 `indicators` 参数

- [ ] **Step 1: 改造 get_kline() 方法**

在 `src/stock_service.py` 顶部加 import：

```python
from services.indicators import calc_rsi, calc_macd, calc_kdj, calc_boll
```

修改 `get_kline` 签名和返回（在函数的 kline 数据构建完毕后，return 之前插入指标计算）：

找到 `get_kline` 方法的定义，改签名为：
```python
async def get_kline(self, symbol: str, period: str = "day", count: int = 120, indicators: str = "") -> dict:
```

在 `resp = {"success": True, "data": records}` 这行之前（records 是 kline 列表），加入指标计算逻辑。找到缓存命中直接返回的位置（如果有缓存且不需要指标时可以走缓存）：

完整的修改逻辑：
1. 如果 `indicators` 参数不为空且非缓存命中路径，从 `records` 中提取 `close/high/low` 数组
2. 根据 `indicators` 参数（逗号分隔）计算对应指标
3. 把结果挂在 `resp["indicators"]` 下

具体改动位置（在 `get_kline` 方法末尾，缓存 set 之前）：

```python
            # 计算技术指标
            ind_data = {}
            if indicators:
                close_prices = [r["close"] for r in records]
                requested = [x.strip().lower() for x in indicators.split(",") if x.strip()]
                if "rsi" in requested:
                    ind_data["rsi"] = {"period": 14, "values": calc_rsi(close_prices)}
                if "macd" in requested:
                    ind_data["macd"] = calc_macd(close_prices)
                if "kdj" in requested:
                    high_prices = [r["high"] for r in records]
                    low_prices = [r["low"] for r in records]
                    ind_data["kdj"] = calc_kdj(high_prices, low_prices, close_prices)
                if "boll" in requested:
                    ind_data["boll"] = calc_boll(close_prices)

            resp = {"success": True, "data": records}
            if ind_data:
                resp["indicators"] = ind_data
```

- [ ] **Step 2: 验证 K线 + 指标**

启动服务器后测试（手动或用 curl）：

```bash
curl "http://localhost:8000/api/stock/kline/600519?period=day&count=30&indicators=rsi,macd"
```

Expected: 响应中包含 `indicators.rsi.values` 和 `indicators.macd.dif`，前期位置为 null，后期有数值。

- [ ] **Step 3: Commit**

```bash
git add src/stock_service.py
git commit -m "feat: K线接口集成技术指标 — 可通过indicators参数请求RSI/MACD/KDJ/BOLL"
```

---

### Task 4: 自选股后端 API

**Files:**
- Create: `src/routers/__init__.py`
- Create: `src/routers/watchlist.py`
- Modify: `src/app.py` — 注册路由

- [ ] **Step 1: 创建 src/routers/__init__.py**

```bash
touch src/routers/__init__.py
```

- [ ] **Step 2: 创建 src/routers/watchlist.py**

```python
"""自选股管理 API — CRUD + 行情批量查询。"""
from fastapi import APIRouter
from pydantic import BaseModel
from database import get_db
from stock_service import StockService
from stock_utils import detect_market, normalize_symbol
import asyncio

router = APIRouter(prefix="/api/watchlist", tags=["watchlist"])
stock_service = StockService()


class WatchlistAdd(BaseModel):
    symbol: str
    market: str = ""
    note: str = ""
    tags: str = ""
    target_price: float | None = None
    alert_price: float | None = None


class WatchlistUpdate(BaseModel):
    note: str | None = None
    tags: str | None = None
    target_price: float | None = None
    alert_price: float | None = None
    sort_order: int | None = None


class ReorderItem(BaseModel):
    id: int
    sort_order: int


@router.post("/add")
async def add(req: WatchlistAdd):
    symbol = normalize_symbol(req.symbol)
    market = req.market or detect_market(req.symbol)
    db = get_db()
    try:
        db.execute(
            """INSERT INTO watchlist (symbol, market, name, note, tags, target_price, alert_price)
               VALUES (?, ?, ?, ?, ?, ?, ?)""",
            (symbol, market, "", req.note, req.tags, req.target_price, req.alert_price),
        )
        db.commit()
        return {"success": True, "id": db.execute("SELECT last_insert_rowid()").fetchone()[0]}
    except Exception as e:
        return {"success": False, "error": str(e)}


@router.delete("/{item_id}")
async def remove(item_id: int):
    db = get_db()
    db.execute("DELETE FROM watchlist WHERE id = ?", (item_id,))
    db.commit()
    return {"success": True}


@router.put("/{item_id}")
async def update(item_id: int, req: WatchlistUpdate):
    db = get_db()
    updates = {}
    if req.note is not None:
        updates["note"] = req.note
    if req.tags is not None:
        updates["tags"] = req.tags
    if req.target_price is not None:
        updates["target_price"] = req.target_price
    if req.alert_price is not None:
        updates["alert_price"] = req.alert_price
    if req.sort_order is not None:
        updates["sort_order"] = req.sort_order
    if not updates:
        return {"success": False, "error": "无更新字段"}
    sets = ", ".join(f"{k}=?" for k in updates)
    values = list(updates.values()) + [item_id]
    db.execute(f"UPDATE watchlist SET {sets} WHERE id=?", values)
    db.commit()
    return {"success": True}


@router.get("/list")
async def get_list():
    db = get_db()
    rows = db.execute(
        "SELECT id, symbol, market, name, note, tags, target_price, alert_price, sort_order, added_at "
        "FROM watchlist ORDER BY sort_order ASC, id ASC"
    ).fetchall()

    items = []
    symbols_to_fetch = []
    for r in rows:
        item = {
            "id": r[0], "symbol": r[1], "market": r[2], "name": r[3],
            "note": r[4], "tags": r[5], "target_price": r[6], "alert_price": r[7],
            "sort_order": r[8], "added_at": r[9], "quote": None,
        }
        items.append(item)
        symbols_to_fetch.append((r[1], r[2]))

    # 批量获取行情
    async def fetch_one(sym, mkt):
        try:
            from global_stock_service import global_stock_service
            if mkt in ("kr", "jp"):
                r = await global_stock_service.get_realtime_quote(sym, mkt)
            else:
                r = await stock_service.get_realtime_quote(sym)
            if r.get("success"):
                return r["data"]
        except Exception:
            pass
        return None

    quotes = await asyncio.gather(*[fetch_one(sym, mkt) for sym, mkt in symbols_to_fetch])
    for item, quote in zip(items, quotes):
        if quote:
            # 统一字段名给前端
            item["quote"] = {
                "price": quote.get("最新价") or quote.get("price"),
                "change_pct": quote.get("涨跌幅") or quote.get("change_pct"),
                "volume": quote.get("成交量") or quote.get("volume"),
                "high": quote.get("最高") or quote.get("high"),
                "low": quote.get("最低") or quote.get("low"),
                "open": quote.get("今开") or quote.get("open"),
                "prev_close": quote.get("昨收") or quote.get("prev_close"),
            }
            # 更新 name（如果有）
            name = quote.get("名称") or quote.get("name")
            if name and not item["name"]:
                item["name"] = name
                db.execute("UPDATE watchlist SET name=? WHERE id=?", (name, item["id"]))

    db.commit()
    return {"success": True, "data": items}


@router.post("/reorder")
async def reorder(items: list[ReorderItem]):
    db = get_db()
    for it in items:
        db.execute("UPDATE watchlist SET sort_order=? WHERE id=?", (it.sort_order, it.id))
    db.commit()
    return {"success": True}
```

- [ ] **Step 3: 在 app.py 中注册路由**

在 `app.py` 顶部的 import 区加上：

```python
from routers.watchlist import router as watchlist_router
```

在 `app = FastAPI(...)` 之后、第一个路由之前加上：

```python
app.include_router(watchlist_router)
```

- [ ] **Step 4: 启动验证**

```bash
curl -X POST "http://localhost:8000/api/watchlist/add" -H "Content-Type: application/json" -d '{"symbol":"600519","note":"白酒","tags":"消费,核心资产"}'
curl "http://localhost:8000/api/watchlist/list"
```

Expected: 添加成功返回 `{"success":true,"id":1}`，列表返回含实时行情。

- [ ] **Step 5: Commit**

```bash
git add src/routers/ src/app.py
git commit -m "feat: 自选股后端API — CRUD + 批量行情"
```

---

### Task 5: 前端自选股 Tab

**Files:**
- Modify: `src/templates/index.html`

- [ ] **Step 1: 侧栏增加"自选"Tab**

在 HTML 中侧栏 tab 按钮区域（搜索 ".market-tab-btn" 附近），新增一个自选按钮：

```html
<button class="btn btn-ghost btn-sm market-tab-btn active" data-market="watchlist">★ 自选</button>
```

- [ ] **Step 2: 自选股页面容器 + 表格**

在主要内容区域，新增自选股面板：

```html
<div id="panel-watchlist" class="panel-content" style="display:block">
  <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
    <h2 style="margin:0">★ 自选股</h2>
    <div>
      <input id="watchlist-add-input" placeholder="输入代码添加自选..." style="padding:6px 10px;width:180px;border:1px solid #444;border-radius:4px;background:#1a1a2e;color:#eee">
      <button class="btn btn-sm btn-primary" onclick="watchlistAdd()" style="margin-left:6px">添加</button>
    </div>
  </div>
  <table class="data-table" id="watchlist-table">
    <thead><tr>
      <th>代码</th><th>名称</th><th>现价</th><th>涨跌幅</th><th>市值</th><th>备注</th><th>操作</th>
    </tr></thead>
    <tbody></tbody>
  </table>
</div>
```

- [ ] **Step 3: JS 逻辑 — 添加/删除/刷新/点击查看**

```javascript
async function watchlistLoad() {
  const json = await (await fetch('/api/watchlist/list')).json();
  if (!json.success) return;
  const tbody = document.querySelector('#watchlist-table tbody');
  tbody.innerHTML = json.data.map(w => {
    const q = w.quote || {};
    const chg = q.change_pct != null ? (q.change_pct>0?'+':'')+q.change_pct.toFixed(2)+'%' : '-';
    const color = q.change_pct > 0 ? '#ef4444' : q.change_pct < 0 ? '#22c55e' : '';
    return `<tr>
      <td><a href="javascript:void(0)" onclick="showStockDetail('${w.symbol}','${w.market}')">${w.symbol}</a></td>
      <td>${w.name||''}</td>
      <td style="color:${color}">${q.price||'-'}</td>
      <td style="color:${color}">${chg}</td>
      <td>${q.volume ? (q.volume/100000000).toFixed(2)+'亿' : '-'}</td>
      <td>${w.note||''}</td>
      <td><button class="btn btn-ghost btn-xs" onclick="watchlistDel(${w.id})">X</button></td>
    </tr>`;
  }).join('');
}

async function watchlistAdd() {
  const inp = document.getElementById('watchlist-add-input');
  const sym = inp.value.trim();
  if (!sym) return;
  const r = await fetch('/api/watchlist/add', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({symbol:sym})});
  const j = await r.json();
  if (j.success) { inp.value = ''; watchlistLoad(); }
  else alert(j.error);
}

async function watchlistDel(id) {
  if (!confirm('确认删除？')) return;
  await fetch('/api/watchlist/'+id, {method:'DELETE'});
  watchlistLoad();
}

// 30s 自动刷新自选行情
setInterval(watchlistLoad, 30000);
watchlistLoad();
```

- [ ] **Step 4: 功能验证**

启动服务器 → 打开浏览器 → 点击 "★ 自选" Tab → 输入 600519 → 点击添加 → 表格行出现实时行情 → 点击 X 删除 → 确认消失

- [ ] **Step 5: Commit**

```bash
git add src/templates/index.html
git commit -m "feat: 前端自选股Tab — 添加/删除/实时行情/30s刷新"
```

---

### Task 6: 模拟买卖后端

**Files:**
- Create: `src/routers/sim.py`
- Modify: `src/app.py` — 注册路由

- [ ] **Step 1: 创建 src/routers/sim.py**

```python
"""模拟交易 API — 开仓/平仓/持仓/统计。"""
from fastapi import APIRouter
from pydantic import BaseModel
from database import get_db
from stock_service import StockService
from stock_utils import detect_market, normalize_symbol

router = APIRouter(prefix="/api/sim", tags=["sim"])
stock_service = StockService()

INITIAL_CAPITAL = 100000.0


class SimOpen(BaseModel):
    symbol: str
    market: str = ""
    direction: str = "long"
    price: float
    quantity: float
    fee: float = 0
    trade_date: str
    note: str = ""


class SimClose(BaseModel):
    id: int
    close_price: float
    close_date: str
    fee: float = 0
    note: str = ""


@router.post("/open")
async def open_trade(req: SimOpen):
    symbol = normalize_symbol(req.symbol)
    market = req.market or detect_market(req.symbol)
    db = get_db()
    db.execute(
        """INSERT INTO sim_trades (symbol, market, direction, price, quantity, fee, trade_date, note)
           VALUES (?, ?, ?, ?, ?, ?, ?, ?)""",
        (symbol, market, req.direction, req.price, req.quantity, req.fee, req.trade_date, req.note),
    )
    db.commit()
    return {"success": True, "id": db.execute("SELECT last_insert_rowid()").fetchone()[0]}


@router.post("/close")
async def close_trade(req: SimClose):
    db = get_db()
    row = db.execute("SELECT * FROM sim_trades WHERE id=? AND status='open'", (req.id,)).fetchone()
    if not row:
        return {"success": False, "error": "未找到该持仓或已平仓"}

    price_col = 4       # price
    qty_col = 5         # quantity
    fee_col = 6         # fee
    pnl = (req.close_price - row[price_col]) * row[qty_col] - row[fee_col] - req.fee
    if row[3] == "short":
        pnl = (row[price_col] - req.close_price) * row[qty_col] - row[fee_col] - req.fee

    db.execute(
        "UPDATE sim_trades SET status='closed', closed_at=?, close_price=?, pnl=? WHERE id=?",
        (req.close_date, req.close_price, round(pnl, 2), req.id),
    )
    db.commit()
    return {"success": True, "pnl": round(pnl, 2)}


@router.get("/positions")
async def get_positions():
    db = get_db()
    rows = db.execute(
        "SELECT id, symbol, market, direction, price, quantity, fee, trade_date, note, created_at "
        "FROM sim_trades WHERE status='open' ORDER BY created_at DESC"
    ).fetchall()

    positions = []
    for r in rows:
        pos = {
            "id": r[0], "symbol": r[1], "market": r[2], "direction": r[3],
            "price": r[4], "quantity": r[5], "fee": r[6], "trade_date": r[7],
            "note": r[8], "created_at": r[9],
        }
        # 获取实时行情算浮动盈亏
        try:
            qr = await stock_service.get_realtime_quote(r[1])
            if qr.get("success"):
                current = qr["data"].get("最新价") or qr["data"].get("price")
                if current:
                    pos["current_price"] = current
                    if r[3] == "short":
                        pos["unrealized_pnl"] = round((r[4] - current) * r[5] - r[6], 2)
                    else:
                        pos["unrealized_pnl"] = round((current - r[4]) * r[5] - r[6], 2)
        except Exception:
            pass
        positions.append(pos)

    return {"success": True, "data": positions}


@router.get("/history")
async def get_history(page: int = 1):
    db = get_db()
    offset = (page - 1) * 20
    rows = db.execute(
        "SELECT * FROM sim_trades WHERE status='closed' ORDER BY closed_at DESC LIMIT 20 OFFSET ?",
        (offset,),
    ).fetchall()
    data = []
    for r in rows:
        data.append({
            "id": r[0], "symbol": r[1], "market": r[2], "direction": r[3],
            "price": r[4], "quantity": r[5], "fee": r[6], "trade_date": r[7],
            "status": r[8], "closed_at": r[9], "close_price": r[10], "pnl": r[11],
            "note": r[12], "created_at": r[13],
        })
    return {"success": True, "data": data}


@router.get("/stats")
async def get_stats():
    db = get_db()
    rows = db.execute(
        "SELECT direction, pnl FROM sim_trades WHERE status='closed' AND pnl IS NOT NULL"
    ).fetchall()
    if not rows:
        return {"success": True, "data": {"total_trades": 0, "win_count": 0, "lose_count": 0, "win_rate": 0, "total_pnl": 0}}

    wins = [r[1] for r in rows if r[1] > 0]
    loses = [r[1] for r in rows if r[1] <= 0]
    total_pnl = sum(r[1] for r in rows)

    # 最大盈亏
    max_win_row = max(rows, key=lambda r: r[1]) if wins else None
    max_lose_row = min(rows, key=lambda r: r[1]) if loses else None

    return {"success": True, "data": {
        "total_trades": len(rows),
        "win_count": len(wins),
        "lose_count": len(loses),
        "win_rate": round(len(wins) / len(rows), 3) if rows else 0,
        "total_pnl": round(total_pnl, 2),
        "avg_win": round(sum(wins)/len(wins), 2) if wins else 0,
        "avg_lose": round(sum(loses)/len(loses), 2) if loses else 0,
        "max_win": {"id": max_win_row[0], "pnl": max_win_row[1]} if max_win_row else None,
        "max_lose": {"id": max_lose_row[0], "pnl": max_lose_row[1]} if max_lose_row else None,
    }}


@router.get("/account")
async def get_account():
    db = get_db()
    closed = db.execute("SELECT COALESCE(SUM(pnl),0) FROM sim_trades WHERE status='closed'").fetchone()[0]
    open_rows = db.execute(
        "SELECT symbol, direction, price, quantity, fee FROM sim_trades WHERE status='open'"
    ).fetchall()

    market_value = 0.0
    for r in open_rows:
        try:
            qr = await stock_service.get_realtime_quote(r[0])
            if qr.get("success"):
                current = qr["data"].get("最新价") or qr["data"].get("price") or r[2]
                market_value += current * r[3]
        except Exception:
            market_value += r[2] * r[3]

    total_equity = INITIAL_CAPITAL + closed
    return {"success": True, "data": {
        "initial_capital": INITIAL_CAPITAL,
        "closed_pnl": round(closed, 2),
        "market_value": round(market_value, 2),
        "total_equity": round(total_equity, 2),
        "total_return_pct": round(closed / INITIAL_CAPITAL * 100, 2),
        "positions_count": len(open_rows),
    }}
```

- [ ] **Step 2: 注册路由**

在 `app.py` 中加：
```python
from routers.sim import router as sim_router
app.include_router(sim_router)
```

- [ ] **Step 3: 验证**

```bash
curl -X POST "http://localhost:8000/api/sim/open" -H "Content-Type: application/json" -d '{"symbol":"600519","price":1650,"quantity":100,"trade_date":"2026-05-06","note":"测试"}'
curl "http://localhost:8000/api/sim/positions"
curl "http://localhost:8000/api/sim/stats"
curl "http://localhost:8000/api/sim/account"
```

- [ ] **Step 4: Commit**

```bash
git add src/routers/sim.py src/app.py
git commit -m "feat: 模拟买卖后端 — 开仓/平仓/持仓/统计/账户概览"
```

---

### Task 7: 交易日记后端

**Files:**
- Create: `src/routers/journal.py`
- Modify: `src/app.py`

- [ ] **Step 1: 创建 src/routers/journal.py**

```python
"""交易日记 API — CRUD。"""
from fastapi import APIRouter
from pydantic import BaseModel
from database import get_db
from stock_utils import detect_market, normalize_symbol

router = APIRouter(prefix="/api/journal", tags=["journal"])


class JournalEntry(BaseModel):
    symbol: str
    market: str = ""
    direction: str = "long"
    entry_date: str = ""
    exit_date: str = ""
    entry_price: float | None = None
    exit_price: float | None = None
    quantity: float | None = None
    reason: str = ""
    reflection: str = ""
    tags: str = ""


@router.post("/add")
async def add(req: JournalEntry):
    symbol = normalize_symbol(req.symbol)
    market = req.market or detect_market(req.symbol)
    db = get_db()
    db.execute(
        """INSERT INTO trade_journal (symbol, market, direction, entry_date, exit_date,
           entry_price, exit_price, quantity, reason, reflection, tags)
           VALUES (?,?,?,?,?,?,?,?,?,?,?)""",
        (symbol, market, req.direction, req.entry_date, req.exit_date,
         req.entry_price, req.exit_price, req.quantity, req.reason, req.reflection, req.tags),
    )
    db.commit()
    return {"success": True, "id": db.execute("SELECT last_insert_rowid()").fetchone()[0]}


@router.delete("/{item_id}")
async def remove(item_id: int):
    db = get_db()
    db.execute("DELETE FROM trade_journal WHERE id=?", (item_id,))
    db.commit()
    return {"success": True}


@router.put("/{item_id}")
async def update(item_id: int, req: JournalEntry):
    symbol = normalize_symbol(req.symbol)
    market = req.market or detect_market(req.symbol)
    db = get_db()
    db.execute(
        """UPDATE trade_journal SET symbol=?, market=?, direction=?, entry_date=?, exit_date=?,
           entry_price=?, exit_price=?, quantity=?, reason=?, reflection=?, tags=? WHERE id=?""",
        (symbol, market, req.direction, req.entry_date, req.exit_date,
         req.entry_price, req.exit_price, req.quantity, req.reason, req.reflection, req.tags, item_id),
    )
    db.commit()
    return {"success": True}


@router.get("/list")
async def get_list(symbol: str = "", tag: str = "", page: int = 1):
    db = get_db()
    where = []
    params = []
    if symbol:
        where.append("symbol LIKE ?")
        params.append(f"%{symbol}%")
    if tag:
        where.append("tags LIKE ?")
        params.append(f"%{tag}%")
    where_clause = " AND ".join(where) if where else "1=1"
    offset = (page - 1) * 20
    rows = db.execute(
        f"SELECT * FROM trade_journal WHERE {where_clause} ORDER BY created_at DESC LIMIT 20 OFFSET ?",
        params + [offset],
    ).fetchall()
    data = []
    for r in rows:
        data.append({
            "id": r[0], "symbol": r[1], "market": r[2], "direction": r[3],
            "entry_date": r[4], "exit_date": r[5], "entry_price": r[6],
            "exit_price": r[7], "quantity": r[8], "reason": r[9],
            "reflection": r[10], "tags": r[11], "created_at": r[12],
        })
    return {"success": True, "data": data}


@router.get("/stats")
async def get_stats():
    db = get_db()
    rows = db.execute("SELECT tags FROM trade_journal WHERE tags != ''").fetchall()
    tag_count = {}
    for (tags_str,) in rows:
        for t in tags_str.split(","):
            t = t.strip()
            if t:
                tag_count[t] = tag_count.get(t, 0) + 1
    sorted_tags = sorted(tag_count.items(), key=lambda x: x[1], reverse=True)
    return {"success": True, "data": [{"tag": k, "count": v} for k, v in sorted_tags]}
```

- [ ] **Step 2: 注册路由**

```python
from routers.journal import router as journal_router
app.include_router(journal_router)
```

- [ ] **Step 3: 验证**

```bash
curl -X POST "http://localhost:8000/api/journal/add" -H "Content-Type: application/json" -d '{"symbol":"600519","direction":"long","entry_date":"2026-05-06","entry_price":1650,"reason":"MA50支撑","tags":"抄底,均线"}'
curl "http://localhost:8000/api/journal/list"
```

- [ ] **Step 4: Commit**

```bash
git add src/routers/journal.py src/app.py
git commit -m "feat: 交易日记后端 — CRUD + 标签统计"
```

---

### Task 8: 博主追踪 + 真实账户 [钩子]

**Files:**
- Create: `src/routers/verify.py`
- Modify: `src/app.py`

- [ ] **Step 1: 创建 src/routers/verify.py（最简版）**

```python
"""博主观点追踪 + 真实账户交易 — 钩子模块，后续迭代。"""
from fastapi import APIRouter, UploadFile
from pydantic import BaseModel
from database import get_db

router = APIRouter(prefix="/api/verify", tags=["verify"])


# ---- 博主观点 ----
class BloggerCallAdd(BaseModel):
    blogger_id: str
    symbol: str
    market: str = ""
    call_type: str = "comment"
    call_date: str
    call_price: float | None = None
    target_price: float | None = None
    article_url: str = ""
    notes: str = ""


@router.post("/blogger-call")
async def add_blogger_call(req: BloggerCallAdd):
    db = get_db()
    db.execute(
        """INSERT INTO blogger_calls (blogger_id, symbol, market, call_type, call_date, call_price, target_price, article_url, notes)
           VALUES (?,?,?,?,?,?,?,?,?)""",
        (req.blogger_id, req.symbol, req.market, req.call_type, req.call_date, req.call_price, req.target_price, req.article_url, req.notes),
    )
    db.commit()
    return {"success": True}


@router.get("/blogger-calls")
async def list_blogger_calls(blogger_id: str = "", symbol: str = ""):
    db = get_db()
    where = []
    params = []
    if blogger_id:
        where.append("blogger_id=?")
        params.append(blogger_id)
    if symbol:
        where.append("symbol LIKE ?")
        params.append(f"%{symbol}%")
    clause = " AND ".join(where) if where else "1=1"
    rows = db.execute(f"SELECT * FROM blogger_calls WHERE {clause} ORDER BY call_date DESC LIMIT 50", params).fetchall()
    return {"success": True, "data": [dict(zip(["id","blogger_id","symbol","market","call_type","call_date","call_price","target_price","article_url","notes","verified","verified_at","created_at"], r)) for r in rows]}


@router.put("/blogger-call/{call_id}")
async def verify_blogger_call(call_id: int, verified: int = 0):
    db = get_db()
    db.execute("UPDATE blogger_calls SET verified=?, verified_at=datetime('now') WHERE id=?", (verified, call_id))
    db.commit()
    return {"success": True}


# ---- 真实账户 ----
class RealTradeAdd(BaseModel):
    symbol: str
    market: str = ""
    direction: str
    price: float
    quantity: float
    fee: float = 0
    trade_date: str
    note: str = ""


@router.post("/real-trade")
async def add_real_trade(req: RealTradeAdd):
    db = get_db()
    db.execute(
        "INSERT INTO real_trades (symbol, market, direction, price, quantity, fee, trade_date, note, source) VALUES (?,?,?,?,?,?,?,?,'manual')",
        (req.symbol, req.market, req.direction, req.price, req.quantity, req.fee, req.trade_date, req.note),
    )
    db.commit()
    return {"success": True}


@router.get("/real-trades")
async def list_real_trades(page: int = 1):
    db = get_db()
    rows = db.execute(
        "SELECT * FROM real_trades ORDER BY trade_date DESC LIMIT 20 OFFSET ?",
        ((page-1)*20,),
    ).fetchall()
    return {"success": True, "data": [dict(zip(["id","symbol","market","direction","price","quantity","fee","trade_date","source","note","created_at"], r)) for r in rows]}


@router.post("/import-csv")
async def import_csv(file: UploadFile):
    """CSV 导入交割单。预期列：symbol, market, direction, price, quantity, fee, trade_date"""
    import csv, io
    content = await file.read()
    reader = csv.DictReader(io.StringIO(content.decode("utf-8")))
    imported, skipped = 0, 0
    db = get_db()
    for row in reader:
        try:
            db.execute(
                "INSERT INTO real_trades (symbol, market, direction, price, quantity, fee, trade_date, source) VALUES (?,?,?,?,?,?,?,'csv_import')",
                (row.get("symbol",""), row.get("market","a"), row.get("direction","long"),
                 float(row.get("price",0)), float(row.get("quantity",0)), float(row.get("fee",0)), row.get("trade_date","")),
            )
            imported += 1
        except Exception:
            skipped += 1
    db.commit()
    return {"success": True, "imported": imported, "skipped": skipped}
```

- [ ] **Step 2: 注册路由**

```python
from routers.verify import router as verify_router
app.include_router(verify_router)
```

- [ ] **Step 3: 验证**

```bash
curl -X POST "http://localhost:8000/api/verify/blogger-call" -H "Content-Type: application/json" -d '{"blogger_id":"test","symbol":"600519","call_type":"buy","call_date":"2026-05-01","call_price":1600}'
curl "http://localhost:8000/api/verify/blogger-calls"
```

- [ ] **Step 4: Commit**

```bash
git add src/routers/verify.py src/app.py
git commit -m "feat: 博主追踪+真实账户钩子 — 最简CRUD+CSV导入"
```

---

### Task 9: 盈亏统计后端

**Files:**
- Create: `src/routers/stats.py`
- Modify: `src/app.py`

- [ ] **Step 1: 创建 src/routers/stats.py**

```python
"""盈亏统计汇总 — 模拟+真实合并。"""
from fastapi import APIRouter
from database import get_db

router = APIRouter(prefix="/api/stats", tags=["stats"])


def _sim_stats(db):
    rows = db.execute("SELECT pnl FROM sim_trades WHERE status='closed' AND pnl IS NOT NULL").fetchall()
    pnls = [r[0] for r in rows]
    if not pnls:
        return {"total_trades": 0, "win_count": 0, "lose_count": 0, "win_rate": 0, "total_pnl": 0}
    wins = [p for p in pnls if p > 0]
    return {
        "total_trades": len(pnls),
        "win_count": len(wins),
        "lose_count": len(pnls) - len(wins),
        "win_rate": round(len(wins) / len(pnls), 3),
        "total_pnl": round(sum(pnls), 2),
    }


def _real_stats(db):
    rows = db.execute("SELECT price, quantity, direction FROM real_trades").fetchall()
    if not rows:
        return {"total_trades": 0, "total_pnl": 0}
    return {"total_trades": len(rows), "total_pnl": 0}  # 无平仓信息时 PnL 为 0


@router.get("/dashboard")
async def dashboard():
    db = get_db()
    sim = _sim_stats(db)
    real = _real_stats(db)
    combined = {
        "total_trades": sim["total_trades"] + real["total_trades"],
        "win_rate": sim["win_rate"],
        "total_pnl": round(sim["total_pnl"] + real["total_pnl"], 2),
    }
    return {"success": True, "data": {"sim": sim, "real": real, "combined": combined}}


@router.get("/monthly")
async def monthly():
    db = get_db()
    rows = db.execute(
        "SELECT substr(closed_at,1,7) as month, SUM(pnl) as mpnl, COUNT(*) as cnt FROM sim_trades WHERE status='closed' AND pnl IS NOT NULL GROUP BY month ORDER BY month DESC LIMIT 12"
    ).fetchall()
    return {"success": True, "data": [{"month": r[0], "pnl": round(r[1], 2), "trades": r[2]} for r in rows]}


@router.get("/by-tag")
async def by_tag():
    """按日记标签分组统计（与模拟交易关联）。"""
    db = get_db()
    rows = db.execute("SELECT tags FROM trade_journal WHERE tags != ''").fetchall()
    # 简化：按标签统计日记数量；后续迭代时关联 sim_trades
    tag_count = {}
    for (tags_str,) in rows:
        for t in tags_str.split(","):
            t = t.strip()
            if t:
                tag_count[t] = tag_count.get(t, 0) + 1
    return {"success": True, "data": [{"tag": k, "count": v} for k, v in sorted(tag_count.items(), key=lambda x: x[1], reverse=True)]}
```

- [ ] **Step 2: 注册路由**

```python
from routers.stats import router as stats_router
app.include_router(stats_router)
```

- [ ] **Step 3: 验证**

```bash
curl "http://localhost:8000/api/stats/dashboard"
```

Expected: `{"success":true,"data":{"sim":{"total_trades":0,...},"real":{"total_trades":0,...},"combined":{...}}}`

- [ ] **Step 4: Commit**

```bash
git add src/routers/stats.py src/app.py
git commit -m "feat: 盈亏统计后端 — 模拟+真实合并dashboard"
```

---

### Task 10: 前端模拟交易页面 + K线指标

**Files:**
- Modify: `src/templates/index.html`

- [ ] **Step 1: 模拟交易面板 HTML**

在 `#panel-watchlist` 同级位置（已有面板区末尾），新增模拟交易面板：

```html
<div id="panel-sim" class="panel-content" style="display:none">
  <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
    <h2 style="margin:0">模拟交易</h2>
    <div>
      <span id="sim-account-info" style="margin-right:16px;font-size:13px;color:#888"></span>
      <button class="btn btn-sm btn-primary" onclick="simShowOpenForm()">开仓</button>
    </div>
  </div>
  <div style="display:flex;gap:16px;margin-bottom:16px;flex-wrap:wrap" id="sim-stats-cards"></div>
  <h3>当前持仓</h3>
  <table class="data-table" id="sim-positions-table">
    <thead><tr>
      <th>代码</th><th>方向</th><th>买入价</th><th>现价</th><th>数量</th><th>浮动盈亏</th><th>日期</th><th>备注</th><th>操作</th>
    </tr></thead>
    <tbody></tbody>
  </table>
  <h3 style="margin-top:20px">已平仓</h3>
  <table class="data-table" id="sim-history-table">
    <thead><tr>
      <th>代码</th><th>方向</th><th>买入价</th><th>卖出价</th><th>数量</th><th>PnL</th><th>日期</th><th>备注</th>
    </tr></thead>
    <tbody></tbody>
  </table>
</div>

<!-- 开仓弹窗 -->
<div id="sim-modal" style="display:none;position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(0,0,0,0.5);z-index:1000;justify-content:center;align-items:center">
  <div style="background:#1a1a2e;padding:24px;border-radius:8px;min-width:360px;border:1px solid #333">
    <h3 style="margin:0 0 16px 0">模拟开仓</h3>
    <div style="display:grid;gap:10px">
      <input id="sim-form-symbol" placeholder="股票代码" style="padding:8px;background:#111;border:1px solid #444;border-radius:4px;color:#eee">
      <div style="display:flex;gap:10px">
        <select id="sim-form-direction" style="padding:8px;background:#111;border:1px solid #444;border-radius:4px;color:#eee;flex:1">
          <option value="long">做多</option><option value="short">做空</option>
        </select>
        <input id="sim-form-price" type="number" step="0.01" placeholder="成交价" style="padding:8px;background:#111;border:1px solid #444;border-radius:4px;color:#eee;flex:1">
      </div>
      <div style="display:flex;gap:10px">
        <input id="sim-form-qty" type="number" placeholder="数量(股)" style="padding:8px;background:#111;border:1px solid #444;border-radius:4px;color:#eee;flex:1">
        <input id="sim-form-fee" type="number" step="0.01" placeholder="手续费" value="5" style="padding:8px;background:#111;border:1px solid #444;border-radius:4px;color:#eee;flex:1">
      </div>
      <input id="sim-form-date" type="date" style="padding:8px;background:#111;border:1px solid #444;border-radius:4px;color:#eee">
      <input id="sim-form-note" placeholder="备注(可选)" style="padding:8px;background:#111;border:1px solid #444;border-radius:4px;color:#eee">
    </div>
    <div style="display:flex;gap:10px;margin-top:16px;justify-content:flex-end">
      <button class="btn btn-ghost btn-sm" onclick="document.getElementById('sim-modal').style.display='none'">取消</button>
      <button class="btn btn-primary btn-sm" onclick="simOpen()">确认开仓</button>
    </div>
  </div>
</div>
```

- [ ] **Step 2: 模拟交易 JS 逻辑**

```javascript
async function simLoad() {
  // 账户信息
  const acct = await (await fetch('/api/sim/account')).json();
  if (acct.success) {
    document.getElementById('sim-account-info').textContent =
      `总权益 ${acct.data.total_equity.toFixed(0)} | 收益率 ${acct.data.total_return_pct}% | 持仓 ${acct.data.positions_count}`;
  }

  // 统计卡片
  const st = await (await fetch('/api/sim/stats')).json();
  if (st.success) {
    const d = st.data;
    document.getElementById('sim-stats-cards').innerHTML = `
      <div class="card" style="flex:1;min-width:100px;text-align:center;padding:12px"><div style="font-size:24px;font-weight:bold">${d.total_trades}</div><div style="color:#888;font-size:12px">总交易</div></div>
      <div class="card" style="flex:1;min-width:100px;text-align:center;padding:12px"><div style="font-size:24px;font-weight:bold;color:#ef4444">${(d.win_rate*100).toFixed(0)}%</div><div style="color:#888;font-size:12px">胜率</div></div>
      <div class="card" style="flex:1;min-width:100px;text-align:center;padding:12px"><div style="font-size:24px;font-weight:bold;color:${d.total_pnl>0?'#ef4444':'#22c55e'}">${d.total_pnl.toFixed(0)}</div><div style="color:#888;font-size:12px">总PnL</div></div>
    `;
  }

  // 持仓
  const pos = await (await fetch('/api/sim/positions')).json();
  document.querySelector('#sim-positions-table tbody').innerHTML = pos.success ? pos.data.map(p => {
    const upnl = p.unrealized_pnl != null ? (p.unrealized_pnl>0?'+':'')+p.unrealized_pnl.toFixed(2) : '-';
    const dir = p.direction === 'long' ? '多' : '空';
    return `<tr>
      <td>${p.symbol}</td><td style="color:${p.direction==='long'?'#ef4444':'#22c55e'}">${dir}</td>
      <td>${p.price}</td><td>${p.current_price||'-'}</td><td>${p.quantity}</td>
      <td style="color:${p.unrealized_pnl>0?'#ef4444':'#22c55e'}">${upnl}</td>
      <td>${p.trade_date}</td><td>${p.note||''}</td>
      <td><button class="btn btn-ghost btn-xs" onclick="simCloseForm(${p.id})">平仓</button></td>
    </tr>`;
  }).join('') : '';

  // 历史
  const hist = await (await fetch('/api/sim/history')).json();
  document.querySelector('#sim-history-table tbody').innerHTML = hist.success ? hist.data.map(h => {
    const dir = h.direction === 'long' ? '多' : '空';
    return `<tr>
      <td>${h.symbol}</td><td style="color:${h.direction==='long'?'#ef4444':'#22c55e'}">${dir}</td>
      <td>${h.price}</td><td>${h.close_price}</td><td>${h.quantity}</td>
      <td style="color:${h.pnl>0?'#ef4444':'#22c55e'}">${(h.pnl>0?'+':'')+h.pnl.toFixed(2)}</td>
      <td>${h.closed_at}</td><td>${h.note||''}</td>
    </tr>`;
  }).join('') : '';
}

function simShowOpenForm() {
  document.getElementById('sim-form-date').value = new Date().toISOString().slice(0,10);
  document.getElementById('sim-modal').style.display = 'flex';
}

async function simOpen() {
  const body = {
    symbol: document.getElementById('sim-form-symbol').value.trim(),
    direction: document.getElementById('sim-form-direction').value,
    price: parseFloat(document.getElementById('sim-form-price').value),
    quantity: parseFloat(document.getElementById('sim-form-qty').value),
    fee: parseFloat(document.getElementById('sim-form-fee').value),
    trade_date: document.getElementById('sim-form-date').value,
    note: document.getElementById('sim-form-note').value,
  };
  if (!body.symbol || !body.price || !body.quantity || !body.trade_date) { alert('请填写完整'); return; }
  const r = await fetch('/api/sim/open', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});
  const j = await r.json();
  if (j.success) {
    document.getElementById('sim-modal').style.display = 'none';
    simLoad();
  } else alert(j.error);
}

function simCloseForm(id) {
  const price = prompt('输入平仓价格:');
  if (!price) return;
  fetch('/api/sim/close', {method:'POST',headers:{'Content-Type':'application/json'},
    body:JSON.stringify({id, close_price:parseFloat(price), close_date:new Date().toISOString().slice(0,10)})})
    .then(r=>r.json()).then(j=>{ if(j.success) simLoad(); else alert(j.error); });
}
```

- [ ] **Step 3: K线指标副图 — ECharts grid 布局**

找到绘制 K 线图的 ECharts 初始化代码（搜索 `echarts.init`），修改 option 增加副图：

```javascript
// 假设 _klineData 是已有的 K 线数据数组 [{date,open,close,high,low,volume},...]
// _indData 是从 /api/stock/kline?indicators=rsi,macd 获取的指标数据

function buildKlineOption(klineData, indData) {
  const dates = klineData.map(d => d.date);
  const closes = klineData.map(d => d.close);

  const option = {
    tooltip: { trigger: 'axis', axisPointer: { type: 'cross' } },
    axisPointer: { link: [{ xAxisIndex: 'all' }] },
    grid: [
      { left: '8%', right: '5%', top: '5%', height: '50%' },   // 主图
      { left: '8%', right: '5%', top: '60%', height: '15%' },  // MACD
      { left: '8%', right: '5%', top: '80%', height: '15%' },  // RSI
    ],
    xAxis: [
      { gridIndex: 0, data: dates, axisLabel: { show: false } },
      { gridIndex: 1, data: dates, axisLabel: { show: false } },
      { gridIndex: 2, data: dates },
    ],
    yAxis: [
      { gridIndex: 0, scale: true },
      { gridIndex: 1, scale: true },
      { gridIndex: 2, min: 0, max: 100 },
    ],
    series: [
      // 主图：K线 + MA
      { name: 'K线', type: 'candlestick', xAxisIndex: 0, yAxisIndex: 0,
        data: klineData.map(d => [d.open, d.close, d.low, d.high]) },
      // MACD 副图
      { name: 'DIF', type: 'line', xAxisIndex: 1, yAxisIndex: 1,
        data: indData?.macd?.dif || [], lineStyle: { color: '#f5d547' } },
      { name: 'DEA', type: 'line', xAxisIndex: 1, yAxisIndex: 1,
        data: indData?.macd?.dea || [], lineStyle: { color: '#ef4444' } },
      { name: 'MACD柱', type: 'bar', xAxisIndex: 1, yAxisIndex: 1,
        data: (indData?.macd?.macd || []).map(v => v != null ? v : 0),
        itemStyle: { color: params => params.value >= 0 ? '#ef4444' : '#22c55e' } },
      // RSI 副图
      { name: 'RSI14', type: 'line', xAxisIndex: 2, yAxisIndex: 2,
        data: indData?.rsi?.values || [], lineStyle: { color: '#06b6d4' },
        markLine: { silent: true, data: [{yAxis:70,lineStyle:{color:'#ef4444',type:'dashed'}},{yAxis:30,lineStyle:{color:'#22c55e',type:'dashed'}}] } },
    ],
  };
  return option;
}
```

修改 K 线数据获取函数，增加 `indicators` 参数：
```javascript
// 原来: fetch(`/api/stock/kline/${symbol}?period=day&count=120`)
// 改为: fetch(`/api/stock/kline/${symbol}?period=day&count=120&indicators=rsi,macd`)
```

顶部加指标开关：
```html
<div style="margin-bottom:8px">
  <label style="margin-right:12px;font-size:13px;cursor:pointer"><input type="checkbox" checked onchange="toggleIndicator('rsi')" id="chk-rsi"> RSI</label>
  <label style="margin-right:12px;font-size:13px;cursor:pointer"><input type="checkbox" checked onchange="toggleIndicator('macd')" id="chk-macd"> MACD</label>
  <label style="margin-right:12px;font-size:13px;cursor:pointer"><input type="checkbox" onchange="toggleIndicator('kdj')" id="chk-kdj"> KDJ</label>
  <label style="margin-right:12px;font-size:13px;cursor:pointer"><input type="checkbox" onchange="toggleIndicator('boll')" id="chk-boll"> BOLL</label>
</div>
```

- [ ] **Step 4: 侧栏增加"模拟交易"Tab**

在侧栏按钮区添加：
```html
<button class="btn btn-ghost btn-sm" onclick="showPanel('sim');simLoad()" style="margin-left:4px">模拟交易</button>
```

- [ ] **Step 5: 功能验证**

浏览器中：切换到模拟交易 Tab → 看到统计卡片和空持仓 → 点"开仓" → 填写600519/1650/100 → 确认 → 持仓出现 → 点"平仓" → 输入1680 → PnL显示+3000。

股票详情页：K 线图出现 MACD 副图（DIF/DEA线+柱）和 RSI 副图（带70/30参考线）。

- [ ] **Step 6: Commit**

```bash
git add src/templates/index.html
git commit -m "feat: 前端模拟交易页面+K线MACD/RSI副图"

---

### Task 11: app.py 渐进拆分

**Files:**
- Create: `src/routers/stock.py`, `src/routers/market.py`, `src/routers/iwencai.py`, `src/routers/blogger.py`, `src/routers/config.py`
- Modify: `src/app.py` — 移除已拆路由实现，只留注册

**注意:** 这是最后一步。所有新路由已在前面 Task 注册完毕。本 Task 把 app.py 里 **已有的旧路由** 挪到对应文件，最终 app.py < 150 行。

- [ ] **Step 1: 创建 src/routers/stock.py**

将 app.py 中以下路由的实现代码完整移到 `src/routers/stock.py`：
- `/api/stock/search`
- `/api/stock/quote/{symbol}`
- `/api/stock/kline/{symbol}`
- `/api/stock/profile/{symbol}`
- `/api/stock/financial/{symbol}`
- `/api/stock/flow/{symbol}`
- `/api/stock/news/{symbol}`
- `/api/stock/announcements/{symbol}`
- `/api/stock/shareholders/{symbol}`
- `/api/stock/financial-rules`
- `/api/agents/personas`

```python
"""股票行情路由 — 从 app.py 迁移。"""
from fastapi import APIRouter
from stock_service import StockService
from stock_utils import detect_market
from global_stock_service import global_stock_service
from iwencai_service import IWencaiService
from agents import list_personas
import json
from pathlib import Path

router = APIRouter()
stock_service = StockService()
wencai_service = IWencaiService()
_RULES_PATH = Path(__file__).parent.parent / "financial_rules.json"


@router.get("/api/stock/search")
async def api_stock_search(keyword: str = ""):
    # --- 从 app.py 逐段复制原实现 ---
    # ...（保持原有逻辑不动）
    pass  # ← 替换为原有代码


# --- 其他 10 个端点同样迁移 ---

@router.get("/api/agents/personas")
async def api_list_personas():
    return {"personas": list_personas()}
```

**关键原则**：
- 复制原 app.py 中的函数体和 import 依赖
- 不改动任何业务逻辑，纯搬移
- 搬完一个端点就测试一个

- [ ] **Step 2-5: 依次创建并注册 market/iwencai/blogger/config 路由**

同样方式：
- `src/routers/market.py` — `/api/market/*`, `/api/fund/*`, `/api/futures/*`
- `src/routers/iwencai.py` — `/api/iwencai/*`
- `src/routers/blogger.py` — `/api/blogger/*` + WebSocket `/ws/*` + 配置测试端点
- `src/routers/config.py` — `/api/config`, 配置模型

每个文件格式相同：
```python
"""xxx路由"""
from fastapi import APIRouter
# ... 原 app.py 中该模块的 import
router = APIRouter()
# ... 原 app.py 中该模块的路由函数（逐段复制）
```

- [ ] **Step 6: app.py 瘦身**

移除已迁移的 import 和路由函数体。最终保留：
```python
"""FastAPI Web应用"""
import asyncio, sys, logging
from pathlib import Path
if sys.platform == "win32":
    asyncio.set_event_loop_policy(asyncio.WindowsProactorEventLoopPolicy())

from fastapi import FastAPI
from fastapi.responses import HTMLResponse
from database import init_db, close_db

app = FastAPI(title="交易感知系统")

# 注册路由
from routers.stock import router as stock_router
from routers.market import router as market_router
from routers.iwencai import router as iwencai_router
from routers.blogger import router as blogger_router
from routers.config import router as config_router
from routers.watchlist import router as watchlist_router
from routers.sim import router as sim_router
from routers.journal import router as journal_router
from routers.verify import router as verify_router
from routers.stats import router as stats_router

app.include_router(stock_router)
app.include_router(market_router)
app.include_router(iwencai_router)
app.include_router(blogger_router)
app.include_router(config_router)
app.include_router(watchlist_router)
app.include_router(sim_router)
app.include_router(journal_router)
app.include_router(verify_router)
app.include_router(stats_router)


@app.get("/", response_class=HTMLResponse)
async def index():
    html_path = Path(__file__).parent / "templates" / "index.html"
    return HTMLResponse(html_path.read_text(encoding="utf-8"))


@app.on_event("startup")
async def _startup():
    init_db()
    asyncio.create_task(StockService.preload_stock_list())

@app.on_event("shutdown")
async def _shutdown():
    close_db()

if __name__ == "__main__":
    import uvicorn
    uvicorn.run("app:app", host="0.0.0.0", port=8000)
```

- [ ] **Step 7: 全量回归测试**

逐项验证：股票搜索 → 行情 → K线 → 财务 → 公告 → 问财 → 期货 → 基金 → 博主抓取 → AI分析 → 自选 → 模拟交易 → 日记 → 统计

- [ ] **Step 8: 确认 app.py < 150 行**

```bash
wc -l src/app.py
```

- [ ] **Step 9: Commit**

```bash
git add src/routers/ src/app.py
git commit -m "refactor: 拆分app.py — 路由按模块迁移到routers/，主文件<150行"
```
```

---

## 异常处理原则（Karpathy 指南）

根据 Karpathy Guidelines：
- 不处理"不可能发生"的异常（已用 try/except 只包裹外部调用）
- 不引入抽象层、工厂模式、配置类（单文件够用）
- 不做防御性编程——信任内部调用，只验证外部边界
- 代码只做需求明确的事，不留"将来可能需要"的扩展点

---

## 验证清单

所有 Task 完成后：
- [ ] 自选股：添加/删除/列表/行情刷新正常
- [ ] K线图：MACD/RSI/KDJ/BOLL 指标显示正确
- [ ] 模拟买卖：开仓→持仓页展示→平仓→PnL 计算→统计正确
- [ ] 交易日记：添加/编辑/删除/列表正常
- [ ] 博主追踪：手动录入/查询正常（钩子）
- [ ] 真实账户：手动录入/CSV导入正常（钩子）
- [ ] 盈亏统计：dashboard/monthly 正确
- [ ] app.py 行数 < 150
- [ ] 已有功能无回归（搜索/行情/K线/财务/公告/问财/期货/基金）
