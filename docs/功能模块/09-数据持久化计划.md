# 数据持久化（SQLite）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将内存 TTLCache 替换为 SQLite 持久化缓存，新增 watchlist 等表，进程重启后数据不丢失

**Architecture:** 新增 `src/database.py` 管理 SQLite 连接和建表；改造 `src/stock_utils.py` 将 `TTLCache` 类替换为 `cache_get/cache_set` 函数，底层读写 SQLite；所有服务模块仅改 import 路径，业务逻辑不变

**Tech Stack:** Python `sqlite3`（标准库）、JSON 序列化、pandas DataFrame 序列化

---

## 文件结构

| 操作 | 文件 | 职责 |
|---|---|---|
| 新增 | `src/database.py` | 连接管理、建表、启动清理 |
| 改造 | `src/stock_utils.py` | 删除 TTLCache，新增 `cache_get/cache_set` |
| 改造 | `src/stock_service.py` | import 从 `cache` → `cache_get, cache_set` |
| 改造 | `src/global_stock_service.py` | 同上 |
| 改造 | `src/iwencai_service.py` | 同上 |
| 改造 | `src/market/fund.py` | 同上 |
| 改造 | `src/app.py` | startup 中调用 `init_db()` |
| 改造 | `.gitignore` | 添加 `data.db` |

---

### Task 1: 创建 `src/database.py`

**Files:**
- Create: `wexin-read-mcp-main/src/database.py`

- [ ] **Step 1: 创建 database.py**

```python
"""SQLite 数据库连接管理 — 单连接，WAL 模式，启动时自动建表。"""

from __future__ import annotations

import logging
import sqlite3
import time
from pathlib import Path

logger = logging.getLogger("database")

_db: sqlite3.Connection | None = None


def get_db() -> sqlite3.Connection:
    """获取全局单例数据库连接。首次调用前必须先调用 init_db()。"""
    global _db
    if _db is None:
        raise RuntimeError("数据库未初始化，请先调用 init_db()")
    return _db


def init_db(db_path: str | None = None) -> None:
    """初始化数据库：创建连接、开启 WAL、建表、清理过期缓存。

    多次调用安全——仅首次生效。
    """
    global _db
    if _db is not None:
        return

    if db_path is None:
        db_path = Path(__file__).parent.parent / "data.db"

    _db = sqlite3.connect(str(db_path), check_same_thread=False)
    _db.execute("PRAGMA journal_mode=WAL")
    _db.execute("PRAGMA busy_timeout=5000")

    _db.executescript("""
        CREATE TABLE IF NOT EXISTS cache (
            key         TEXT PRIMARY KEY,
            value       TEXT NOT NULL,
            expires_at  REAL NOT NULL
        );

        CREATE TABLE IF NOT EXISTS watchlist (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            symbol      TEXT NOT NULL,
            market      TEXT NOT NULL,
            name        TEXT,
            added_at    TEXT NOT NULL DEFAULT (datetime('now')),
            note        TEXT DEFAULT '',
            sort_order  INTEGER DEFAULT 0,
            UNIQUE(symbol, market)
        );

        CREATE TABLE IF NOT EXISTS portfolios (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            name        TEXT NOT NULL,
            created_at  TEXT NOT NULL DEFAULT (datetime('now')),
            updated_at  TEXT NOT NULL DEFAULT (datetime('now'))
        );

        CREATE TABLE IF NOT EXISTS positions (
            id            INTEGER PRIMARY KEY AUTOINCREMENT,
            portfolio_id  INTEGER NOT NULL REFERENCES portfolios(id),
            symbol        TEXT NOT NULL,
            market        TEXT NOT NULL,
            name          TEXT,
            shares        REAL NOT NULL,
            buy_price     REAL NOT NULL,
            buy_date      TEXT NOT NULL,
            created_at    TEXT NOT NULL DEFAULT (datetime('now')),
            UNIQUE(portfolio_id, symbol, market)
        );

        CREATE TABLE IF NOT EXISTS backtests (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            name        TEXT,
            symbol      TEXT NOT NULL,
            market      TEXT NOT NULL,
            strategy    TEXT NOT NULL,
            params      TEXT NOT NULL,
            start_date  TEXT NOT NULL,
            end_date    TEXT NOT NULL,
            result      TEXT NOT NULL,
            created_at  TEXT NOT NULL DEFAULT (datetime('now'))
        );
    """)

    # 清理过期缓存
    deleted = _db.execute(
        "DELETE FROM cache WHERE expires_at < ?", (time.time(),)
    ).rowcount
    if deleted:
        logger.info(f"清理过期缓存: {deleted} 条")

    _db.commit()
    logger.info("数据库初始化完成")
```

- [ ] **Step 2: 验证独立运行不报错**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db, get_db
init_db()
db = get_db()
tables = db.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()
print('Tables:', [t[0] for t in tables])
"
```

Expected: `Tables: ['cache', 'watchlist', 'portfolios', 'positions', 'backtests']`

- [ ] **Step 3: Commit**

```bash
git add wexin-read-mcp-main/src/database.py
git commit -m "feat: add SQLite database module with connection management"
```

---

### Task 2: 改造 `src/stock_utils.py` — 删除 TTLCache，新增 cache_get/set

**Files:**
- Modify: `wexin-read-mcp-main/src/stock_utils.py`

- [ ] **Step 1: 替换 TTLCache 类和相关代码**

删除 `stock_utils.py` 第 97-121 行（`TTL_REALTIME` 之后的 TTLCache 类和 `cache = TTLCache()` 实例），替换为以下内容：

```python
# ─── 持久化缓存（SQLite）───

import json
from database import get_db


def cache_get(key: str):
    """从 cache 表读取缓存值。过期返回 None 并删除旧行。"""
    db = get_db()
    row = db.execute(
        "SELECT value, expires_at FROM cache WHERE key = ?", (key,)
    ).fetchone()
    if not row:
        return None
    raw, expires_at = row
    if time.time() >= expires_at:
        db.execute("DELETE FROM cache WHERE key = ?", (key,))
        db.commit()
        return None
    return _deserialize(raw)


def cache_set(key: str, value: object, ttl: int = TTL_DAILY) -> None:
    """写入 cache 表，自动处理 DataFrame 序列化。"""
    db = get_db()
    raw = _serialize(value)
    db.execute(
        "INSERT OR REPLACE INTO cache (key, value, expires_at) VALUES (?, ?, ?)",
        (key, raw, time.time() + ttl),
    )
    db.commit()


def _serialize(value: object) -> str:
    import pandas as pd
    if isinstance(value, pd.DataFrame):
        return json.dumps(
            {"__type": "DataFrame", "data": value.to_dict(orient="records")},
            ensure_ascii=False,
        )
    return json.dumps(value, ensure_ascii=False, default=str)


def _deserialize(raw: str):
    data = json.loads(raw)
    if isinstance(data, dict) and data.get("__type") == "DataFrame":
        import pandas as pd
        return pd.DataFrame(data["data"])
    return data
```

同时删除文件末尾的 `cache = TTLCache()` 这一行。

TTL 常量（`TTL_REALTIME` 等，第 99-102 行）保留不动。

- [ ] **Step 2: 验证 stock_utils 可以导入**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db; init_db()
from src.stock_utils import cache_get, cache_set, TTL_REALTIME
# 测试基本读写
cache_set('test_key', {'a': 1}, TTL_REALTIME)
result = cache_get('test_key')
assert result == {'a': 1}, f'Expected {{a: 1}}, got {result}'
print('OK: cache_get/cache_set works')
"
```

Expected: `OK: cache_get/cache_set works`

- [ ] **Step 3: 验证 DataFrame 序列化**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db; init_db()
from src.stock_utils import cache_get, cache_set, TTL_DAILY
import pandas as pd

df = pd.DataFrame({'code': ['000001', '000002'], 'name': ['平安银行', '万科A']})
cache_set('test_df', df, TTL_DAILY)
result = cache_get('test_df')
assert isinstance(result, pd.DataFrame), f'Expected DataFrame, got {type(result)}'
assert result.equals(df), f'Data mismatch:\n{result}\n!=\n{df}'
print('OK: DataFrame roundtrip works')
"
```

Expected: `OK: DataFrame roundtrip works`

- [ ] **Step 4: 验证过期机制**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db; init_db()
from src.stock_utils import cache_get, cache_set

cache_set('expire_test', 'hello', ttl=-1)  # 已过期
result = cache_get('expire_test')
assert result is None, f'Expected None for expired key, got {result}'
print('OK: expired key returns None')
"
```

Expected: `OK: expired key returns None`

- [ ] **Step 5: Commit**

```bash
git add wexin-read-mcp-main/src/stock_utils.py
git commit -m "refactor: replace in-memory TTLCache with SQLite-backed cache_get/cache_set"
```

---

### Task 3: 更新 `.gitignore`

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: 添加 data.db**

在 `.gitignore` 末尾添加一行 `data.db`：

```
.venv/
.DS_Store
.claude/
__pycache__/
*.pyc
data.db
```

- [ ] **Step 2: 验证 git 忽略**

```bash
cd /Users/wangjun/Desktop/股票信息 && git status --short -- wexin-read-mcp-main/data.db
```

Expected: 无输出（文件被忽略）

- [ ] **Step 3: Commit**

```bash
git add .gitignore
git commit -m "chore: add data.db to .gitignore"
```

---

### Task 4: 改造 `src/app.py` — 启动时初始化数据库

**Files:**
- Modify: `wexin-read-mcp-main/src/app.py`

- [ ] **Step 1: 在 startup 中添加 init_db() 调用**

在 `app.py` 文件顶部的 import 区域，添加：

```python
from database import init_db
```

在 `_startup()` 函数（约第 1024 行）的开头添加 `init_db()`：

```python
@app.on_event("startup")
async def _startup():
    """启动时预加载数据。"""
    init_db()  # 初始化 SQLite 数据库
    asyncio.create_task(StockService.preload_stock_list())
    # ... 后续代码不变
```

**具体改动**：在第 1025 行 `"""启动时预加载数据。"""` 之后、`asyncio.create_task(StockService.preload_stock_list())` 之前插入 `init_db()`。

- [ ] **Step 2: 验证应用可以启动**

```bash
cd wexin-read-mcp-main && timeout 5 python src/app.py 2>&1 || true
```

Expected: 应用启动无异常，日志中出现 "数据库初始化完成" 或类似信息

- [ ] **Step 3: Commit**

```bash
git add wexin-read-mcp-main/src/app.py
git commit -m "feat: initialize SQLite database on app startup"
```

---

### Task 5: 改造 `src/stock_service.py` — 切换 cache 导入

**Files:**
- Modify: `wexin-read-mcp-main/src/stock_service.py`

- [ ] **Step 1: 修改 import 语句**

定位 `stock_service.py` 第 27 行左右的 import 块，将：

```python
from stock_utils import (
    ...
)
```

中的导入项从 `cache` 改为 `cache_get, cache_set`，并确认 TTL 常量仍在导入列表中。

具体来说，找到类似这样的行：
```python
from stock_utils import (
    normalize_symbol, detect_market, get_exchange, format_with_exchange,
    TTL_REALTIME, TTL_REALTIME_REFRESH, TTL_DAILY, TTL_COMPANY,
    cache,
)
```

改为：
```python
from stock_utils import (
    normalize_symbol, detect_market, get_exchange, format_with_exchange,
    TTL_REALTIME, TTL_REALTIME_REFRESH, TTL_DAILY, TTL_COMPANY,
    cache_get, cache_set,
)
```

- [ ] **Step 2: 替换所有 cache.get → cache_get, cache.set → cache_set**

在 `stock_service.py` 中执行全局替换：
- `cache.get(` → `cache_get(`
- `cache.set(` → `cache_set(`

从前面 grep 结果可知：共 9 处 `cache.get` 和 10 处 `cache.set`。

- [ ] **Step 3: 验证导入无报错**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db; init_db()
from src.stock_service import StockService
print('OK: stock_service imports successfully')
"
```

Expected: `OK: stock_service imports successfully`

- [ ] **Step 4: Commit**

```bash
git add wexin-read-mcp-main/src/stock_service.py
git commit -m "refactor: switch stock_service cache from TTLCache to SQLite"
```

---

### Task 6: 改造 `src/global_stock_service.py` — 切换 cache 导入

**Files:**
- Modify: `wexin-read-mcp-main/src/global_stock_service.py`

- [ ] **Step 1: 修改 import 并替换调用**

修改 `global_stock_service.py` 第 15 行 import：
```python
# 改前
from stock_utils import TTL_COMPANY, TTL_DAILY, TTL_REALTIME, cache
# 改后
from stock_utils import TTL_COMPANY, TTL_DAILY, TTL_REALTIME, cache_get, cache_set
```

全局替换：
- `cache.get(` → `cache_get(`（4 处）
- `cache.set(` → `cache_set(`（4 处）

- [ ] **Step 2: 验证导入**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db; init_db()
from src.global_stock_service import global_stock_service
print('OK: global_stock_service imports successfully')
"
```

Expected: `OK: global_stock_service imports successfully`

- [ ] **Step 3: Commit**

```bash
git add wexin-read-mcp-main/src/global_stock_service.py
git commit -m "refactor: switch global_stock_service cache from TTLCache to SQLite"
```

---

### Task 7: 改造 `src/iwencai_service.py` — 切换 cache 导入

**Files:**
- Modify: `wexin-read-mcp-main/src/iwencai_service.py`

- [ ] **Step 1: 修改 import 并替换调用**

修改 `iwencai_service.py` 第 13 行 import：
```python
# 改前
from stock_utils import TTL_COMPANY, TTL_DAILY, TTL_REALTIME, cache
# 改后
from stock_utils import TTL_COMPANY, TTL_DAILY, TTL_REALTIME, cache_get, cache_set
```

全局替换：
- `cache.get(` → `cache_get(`（5 处）
- `cache.set(` → `cache_set(`（5 处）

- [ ] **Step 2: 验证导入**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db; init_db()
from src.iwencai_service import IWencaiService
print('OK: iwencai_service imports successfully')
"
```

Expected: `OK: iwencai_service imports successfully`

- [ ] **Step 3: Commit**

```bash
git add wexin-read-mcp-main/src/iwencai_service.py
git commit -m "refactor: switch iwencai_service cache from TTLCache to SQLite"
```

---

### Task 8: 改造 `src/market/fund.py` — 切换 cache 导入

**Files:**
- Modify: `wexin-read-mcp-main/src/market/fund.py`

- [ ] **Step 1: 修改 import 并替换调用**

修改 `market/fund.py` 第 13 行 import：
```python
# 改前
from stock_utils import TTL_COMPANY, cache
# 改后
from stock_utils import TTL_COMPANY, cache_get, cache_set
```

全局替换：
- `cache.get(` → `cache_get(`（4 处）
- `cache.set(` → `cache_set(`（5 处）

- [ ] **Step 2: 验证导入**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db; init_db()
from src.market.fund import FundProvider
print('OK: fund module imports successfully')
"
```

Expected: `OK: fund module imports successfully`

- [ ] **Step 3: Commit**

```bash
git add wexin-read-mcp-main/src/market/fund.py
git commit -m "refactor: switch fund cache from TTLCache to SQLite"
```

---

### Task 9: 端到端集成验证

- [ ] **Step 1: 启动应用并验证核心功能**

```bash
cd wexin-read-mcp-main && python -c "
from src.database import init_db, get_db
init_db()

# 模拟完整的缓存读写流程
import pandas as pd
from src.stock_utils import cache_get, cache_set, TTL_REALTIME, TTL_DAILY

# 1. 模拟行情缓存（dict 类型）
quote = {'symbol': '000001', 'price': 12.50, 'change': 2.3}
cache_set('quote:000001', quote, TTL_REALTIME)
assert cache_get('quote:000001') == quote

# 2. 模拟 K 线缓存（DataFrame 类型）
df = pd.DataFrame({
    'date': ['2026-01-01', '2026-01-02'],
    'open': [10.0, 10.5],
    'close': [10.5, 10.3],
})
cache_set('kline:000001:day:250', df, TTL_DAILY)
result = cache_get('kline:000001:day:250')
assert isinstance(result, pd.DataFrame)
assert len(result) == 2

# 3. 验证重启后缓存持久化
print('All data persisted in data.db, restart the app to verify')
print('OK: all integration checks passed')
"
```

Expected: `OK: all integration checks passed`

- [ ] **Step 2: 检查无遗漏的 cache 引用**

```bash
cd wexin-read-mcp-main && grep -rn "from stock_utils import.*cache[^_]|import cache" src/ --include="*.py" || echo "No stale cache imports found"
```

Expected: `No stale cache imports found` 或只有 `stock_utils.py` 内部的引用

- [ ] **Step 3: 确认 data.db 在 .gitignore 中**

```bash
cd /Users/wangjun/Desktop/股票信息 && git status --short wexin-read-mcp-main/data.db
```

Expected: 无输出

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: verify data persistence integration"
```

---

## 完成后检查清单

- [ ] `data.db` 在项目根目录自动生成
- [ ] `data.db` 被 `.gitignore` 忽略
- [ ] 应用启动时自动建表
- [ ] 过期缓存自动清理
- [ ] 行情/财务缓存重启后不丢失
- [ ] 所有原有 API 功能正常
- [ ] `bloggers.json` 读写保持不动
